(()=>{
'use strict';
if(window.PI_COURSE_V4_ENHANCED)return;
const courses=window.PI_COURSES_V2||[];
const A=(title,detail)=>({title,detail});
const T=(problem,fix)=>({problem,fix});
const C=(label,code,result)=>({label,code,result});
const V=(scene,caption,photo=null,photos=null)=>({scene,caption,photo,photos});
const S=(title,why,time,visual,actions,success,warning='',trouble=[],commands=[])=>({title,why,time,visual,actions,success,warning,trouble,commands});
const commonFoundation=(course)=>S('Install the common Raspberry Pi software tools',`Before installing ${course.title}-specific software, set up the common tools used throughout this course library. This gives you a known Terminal, Git, browser, Python, and local web-serving foundation.`,'20–35 min',V('pi-os','Raspberry Pi OS Desktop with Terminal open. Git, Python, curl, Chromium, and a project folder are ready before project-specific software is installed.'),[A('Confirm you are on Raspberry Pi OS Desktop','Open Terminal from the desktop. These courses assume the full Desktop image, not Raspberry Pi OS Lite, because several projects use Chromium or a graphical app.'),A('Update package information first','Run the update command below and let it finish. If it reports a repository or network error, fix that before installing anything else.'),A('Install the shared command-line tools','Install Git, curl, Python 3, Python virtual environments, and Chromium. The exact browser package name can vary by Raspberry Pi OS release, so the command below tries the current package first and falls back to the older package name.'),A('Create one projects folder','Keep project software in ~/pi-projects so you always know where files live. Do not scatter copies across Desktop, Downloads, and home.'),A('Verify the tools','Check Git, Python, and Chromium versions. A version number is enough; you do not need to memorize it.'),A('Bookmark the built-in Raspberry Pi Reference page','In Pi Command Center, open Raspberry Pi Reference from the left navigation. It contains the board orientation, GPIO map, DSI safety, Imager checklist, Terminal basics, and official software links used by the courses.')],[`Terminal opens normally.`,`Git and Python print version numbers.`,`Chromium launches from the desktop or command line.`,`The ~/pi-projects folder exists.`],'Do not paste commands from random install scripts when an official project provides manual installation instructions. Keep your Pi powered from a stable USB-C supply during package installs.',[T('apt says it cannot resolve a host','Confirm Wi-Fi works in Chromium, then rerun sudo apt update.'),T('Chromium package is not found','Run apt search chromium and install the package named chromium or chromium-browser for your OS release.'),T('A package operation was interrupted','Run sudo dpkg --configure -a, then sudo apt -f install, then retry the install.')],[C('Update package lists','sudo apt update','The command ends without unresolved repository errors.'),C('Install common tools','sudo apt install -y git curl python3 python3-venv python3-pip chromium || sudo apt install -y git curl python3 python3-venv python3-pip chromium-browser','Packages install successfully.'),C('Create projects folder','mkdir -p ~/pi-projects','The folder is created without output or error.'),C('Verify versions','git --version && python3 --version && (chromium --version || chromium-browser --version)','Version lines appear for Git, Python, and Chromium.')]);
const companionPhase=(course,file,label)=>S(`Install the included ${label} companion software`,`This project does not require you to invent an application from scratch. Pi Command Center includes a working starter app for ${course.title}. Install the repository locally, start its private local web server, and test the exact project page before configuring kiosk startup.`,'20–35 min',V('dashboard-shell',`${course.title} companion software running locally in Chromium from 127.0.0.1. The app is served from the cloned Pi Command Center repository, not from an unknown third-party download.`),[A('Clone the Pi Command Center repository once','If ~/pi-projects/pi-command-center already exists from another course, do not clone a second copy. Pull the latest version instead.'),A('Start the local project server manually','Run the server command below. Keep that Terminal window open for this first test.'),A(`Open the ${label} page`,`In Chromium, open http://127.0.0.1:8088/software/${file}. Use 127.0.0.1 so the project still works even if your internet connection drops.`),A('Test every control before autostart','Click or tap every visible control. Resize the browser once to confirm the layout remains readable on the Pi display.'),A('Create the user service for the local server','Use the supplied installer script so the local server starts automatically after login. The installer does not expose the server to your network; it binds only to 127.0.0.1.'),A('Reboot once and retest','After the installer finishes, reboot and confirm the local project URL loads again. Only then continue to the project-specific kiosk phase.')],[`http://127.0.0.1:8088/software/${file} loads on the Pi.`,`The project controls work before kiosk mode is enabled.`,`After reboot, the local server returns automatically.`],'Do not change the server bind address to 0.0.0.0 unless you intentionally want other devices on your network to reach it.',[T('git clone says the folder already exists','Run cd ~/pi-projects/pi-command-center && git pull instead of cloning again.'),T('The browser says connection refused','Confirm the local server is running. Test systemctl --user status pi-project-server after you install the service.'),T('Port 8088 is already in use','Run ss -ltnp | grep 8088 to identify the existing service. Stop the duplicate rather than starting multiple project servers.')],[C('Clone or update the project software',`if [ -d ~/pi-projects/pi-command-center/.git ]; then cd ~/pi-projects/pi-command-center && git pull; else git clone https://github.com/nikkimasani/pi-command-center.git ~/pi-projects/pi-command-center; fi`,'The repository exists at ~/pi-projects/pi-command-center.'),C('Test the local server','cd ~/pi-projects/pi-command-center && python3 -m http.server 8088 --bind 127.0.0.1','Terminal reports Serving HTTP on 127.0.0.1 port 8088.'),C('Install the persistent local server','cd ~/pi-projects/pi-command-center && bash software/install-local-server.sh','The user service is enabled and started.'),C('Check server status','systemctl --user --no-pager status pi-project-server','Status shows active (running).')]);
const softwareById={
'smart-mirror':course=>S('Install MagicMirror² and prove the software before framing it','The Smart Mirror uses MagicMirror², the established open-source modular smart-mirror platform. Install it from the official MagicMirrorOrg repository, run the default configuration first, and only then customize modules or hide it behind the mirror.','35–75 min',V('mirror-ui','MagicMirror² running on the Raspberry Pi desktop with the default layout before custom styling or physical enclosure work.'),[A('Confirm Raspberry Pi OS Desktop is current','MagicMirror² requires a graphical desktop for its normal Electron mode. Do not use Raspberry Pi OS Lite for this course.'),A('Check Node.js before cloning','Run node --version. MagicMirror² can change its minimum Node requirement between releases; if installation reports your Node is too old, follow the current Node requirement linked in the Raspberry Pi Reference page rather than forcing the install.'),A('Clone the official MagicMirror² repository','Use the exact MagicMirrorOrg repository command below. Avoid old installer commands copied from outdated tutorials.'),A('Run the official installer','From ~/MagicMirror, run node --run install-mm. It can sit quietly for several minutes; do not interrupt it just because the Terminal looks idle.'),A('Create your working configuration','Copy config/config.js.sample to config/config.js before making changes. Keep the sample intact as a reference.'),A('Start MagicMirror² manually','Run node --run start. Verify the MagicMirror window appears and remains stable before setting up autostart.'),A('Learn the recovery shortcut','If you need developer tools, use Ctrl+Shift+I. If the app fails to launch later, start it manually from Terminal so you can see the error output.'),A('Do not add third-party modules yet','First complete the mirror using only the built-in modules. Add community modules one at a time after the base build cold-boots reliably.')],['~/MagicMirror exists.','node --run install-mm completes.','config/config.js exists.','node --run start opens MagicMirror² successfully.'],'Use the officially supported manual MagicMirror² installation. Community one-line install scripts are convenient but are not the core project’s supported install path.',[T('node is not installed','Install a Node.js version that satisfies the current MagicMirror² release requirement, then rerun node --version before continuing.'),T('install-mm appears frozen','Give it time. On Raspberry Pi hardware dependency installation can take several minutes with little Terminal output.'),T('MagicMirror opens to a black or error screen','Run node --run start:dev and inspect the developer console. Restore the sample config if the error began after editing config.js.')],[C('Check Node and Git','node --version && git --version','Both commands print version numbers.'),C('Clone official MagicMirror²','cd ~ && git clone https://github.com/MagicMirrorOrg/MagicMirror.git','A MagicMirror folder is created in your home directory.'),C('Install MagicMirror²','cd ~/MagicMirror && node --run install-mm','Dependencies install without a fatal error.'),C('Create config','cd ~/MagicMirror && cp config/config.js.sample config/config.js','config/config.js exists.'),C('Start MagicMirror²','cd ~/MagicMirror && node --run start','The MagicMirror² Electron window opens.')]),
'dashboard':course=>companionPhase(course,'project-suite.html?app=dashboard','Personal Dashboard'),
'ai-terminal':course=>S('Set up the Mini AI Terminal software safely','The terminal should never put an API secret into browser JavaScript. Start with the secure browser-based option, then use an optional local server-side bridge only if you have an API key and understand its cost and privacy implications.','25–45 min',V('terminal-secure','AI Terminal with a local launcher and no API key visible in browser source.'),[A('Choose your AI mode','For the simplest setup, use ChatGPT in Chromium and pin it as the terminal home page. For a custom API terminal, keep the API call server-side.'),A('Never paste an API key into an HTML file','Browser files are readable by anyone with access to the page. API keys belong in an environment variable or protected server-side configuration.'),A('Clone the companion repository','The included AI terminal landing page provides large touch targets and a clear launch path without pretending to be the API itself.'),A('Test keyboard and touch','Type a long prompt, use Backspace, Enter, copy/paste, and scroll. Fix input issues before mounting the keyboard.'),A('Keep a normal browser escape route','Do not make kiosk mode impossible to exit. Keep a documented keyboard shortcut or maintenance button.'),A('Only enable automatic login on a physically trusted device','If other people can reach the terminal, use a separate browser profile and avoid storing sensitive conversations locally.')],['The terminal can open the chosen AI service.','No API key appears in page source or GitHub.','Keyboard, copy/paste, and scrolling work.'],'API usage can incur charges. Never commit an API key to GitHub or store it in client-side JavaScript.',[T('The AI site is slow on the Pi','Use Chromium with unnecessary tabs closed. Network latency can still dominate.'),T('You want an API-powered custom terminal','Use a server-side bridge and store the key only in an environment variable.')],[C('Clone or update companion files',`if [ -d ~/pi-projects/pi-command-center/.git ]; then cd ~/pi-projects/pi-command-center && git pull; else git clone https://github.com/nikkimasani/pi-command-center.git ~/pi-projects/pi-command-center; fi`,'Repository is present locally.'),C('Install the local server','cd ~/pi-projects/pi-command-center && bash software/install-local-server.sh','The local server starts and is enabled for future logins.'),C('Open the included terminal launcher',"chromium 'http://127.0.0.1:8088/software/project-suite.html?app=ai'",'The local AI Terminal launcher opens after the local server is running.')]),
'cyberdeck':course=>companionPhase(course,'project-suite.html?app=cyberdeck','Cyberdeck Launcher'),
'home-panel':course=>S('Connect the panel to Home Assistant and create a kiosk URL','The Pi panel is a display client for an existing Home Assistant instance. You do not need to reinstall Home Assistant on this Pi just to show a dashboard.','20–40 min',V('home-ui','Chromium displaying a dedicated Home Assistant dashboard URL in full-screen kiosk mode.'),[A('Confirm Home Assistant already works on another device','From your phone or laptop, open your Home Assistant instance and verify the intended dashboard before configuring the Pi.'),A('Create a dedicated dashboard view','Make a panel-friendly view with large controls. Avoid putting admin settings or destructive controls on the always-visible screen.'),A('Create a dedicated Home Assistant user if appropriate','A wall panel usually does not need administrator privileges. Use the least privilege that still allows the dashboard to function.'),A('Open the exact dashboard URL on the Pi','Sign in once in Chromium and confirm the desired dashboard is the initial page.'),A('Test touch targets','Toggle a harmless light or test entity, navigate between intended views, and confirm scrolling does not expose unwanted admin pages.'),A('Record the dashboard URL in your private notes','The course note field is fine for the URL. Do not store your Home Assistant password in the course or repository.'),A('Only then configure Chromium kiosk startup','The later kiosk phase should launch the tested URL, not the Home Assistant home page.')],['The exact Home Assistant dashboard loads on the Pi.','The panel user can operate intended controls.','The dashboard URL is known and tested before kiosk autostart.'],'Do not embed Home Assistant credentials in a URL, shell script, HTML file, or GitHub repository.',[T('The page loads but asks for login after every reboot','Check Chromium profile persistence and cookie settings. Do not work around it by putting credentials in the URL.'),T('Controls are too small','Create a dedicated dashboard layout for the panel rather than zooming the entire browser excessively.')],[C('Open Home Assistant locally','chromium http://homeassistant.local:8123','Home Assistant opens. Replace the URL with your actual instance if needed.'),C('Find the Pi IP address','hostname -I','The Pi prints its local network address for troubleshooting.')]),
'electronics-lab':course=>S('Install GPIO tools and run the included safe hardware test','Use gpiozero with the Raspberry Pi GPIO stack instead of manipulating pins blindly. Start with an LED and resistor on a known GPIO, then add buttons or sensors only after that test passes.','20–35 min',V('breadboard-blink','Raspberry Pi GPIO test script controlling one LED through a resistor, with the Pi powered off during wiring changes.'),[A('Install the Raspberry Pi GPIO Python packages','Use the distro packages below so they match your Raspberry Pi OS release.'),A('Clone the companion repository','The included electronics-lab.py script provides LED blink and button tests without requiring you to write code first.'),A('Wire only while powered off','Connect the resistor and LED exactly as shown in the Raspberry Pi Reference GPIO section. Never connect an LED directly between a GPIO pin and ground.'),A('Run the LED test','Start with the default GPIO defined in the script and confirm the LED blinks.'),A('Stop the script before rewiring','Press Ctrl+C, shut down or disconnect power, then change the circuit.'),A('Run the button test only after the LED test works','This separates software problems from wiring problems.')],['gpiozero imports without error.','The LED test controls only the intended LED.','You can stop the script cleanly with Ctrl+C.'],'GPIO pins are 3.3 V logic. Never feed 5 V into a GPIO input. Always use a current-limiting resistor with a bare LED.',[T('ModuleNotFoundError: gpiozero','Install python3-gpiozero and rerun the script with python3.'),T('The LED never lights','Power off, verify LED polarity, resistor placement, GPIO pin number, and ground connection.')],[C('Install GPIO packages','sudo apt install -y python3-gpiozero python3-lgpio','Packages install successfully.'),C('Clone or update companion files',`if [ -d ~/pi-projects/pi-command-center/.git ]; then cd ~/pi-projects/pi-command-center && git pull; else git clone https://github.com/nikkimasani/pi-command-center.git ~/pi-projects/pi-command-center; fi`,'Repository exists locally.'),C('Run LED test','python3 ~/pi-projects/pi-command-center/software/electronics-lab.py led','The LED blinks until you press Ctrl+C.')]),
'pomodoro':course=>companionPhase(course,'project-suite.html?app=pomodoro','Pomodoro Station'),
'glance':course=>companionPhase(course,'project-suite.html?app=desk','Desk Info Center'),
'photo-frame':course=>companionPhase(course,'project-suite.html?app=photo','Digital Photo Frame'),
'magic-frame':course=>S('Install the Magic Mirror Frame software stack','The hybrid frame uses a known-good MagicMirror² installation for dashboard mode plus the included Pi Command Center photo/sleep controller for dormant and photo modes. Build and test each software layer independently before adding wake hardware.','45–90 min',V('magic-prereq','Three tested software states on the same Pi: MagicMirror² dashboard, pure-black Sleep mode, and the local Photo Frame controller.'),[A('Install MagicMirror² using the official manual method','If Smart Mirror is already complete on this Pi, do not reinstall. Confirm ~/MagicMirror starts successfully.'),A('Install the Pi Command Center companion files','Clone or update the repository under ~/pi-projects/pi-command-center.'),A('Start the private local server','Use software/install-local-server.sh so the Sleep and Photo controller pages are available at 127.0.0.1:8088.'),A('Test the pure-black sleep page','Open /software/project-suite.html?app=magic and select Sleep. Inspect the mirror in a dark room and confirm there is no bright text, cursor, or browser chrome after kiosk mode is enabled.'),A('Test Photo mode','Add sample images in the Photo Frame companion app and verify the selected image fills the intended display area.'),A('Test MagicMirror² dashboard mode separately','Run ~/MagicMirror with node --run start and verify the standard mirror dashboard still works.'),A('Record the three recovery URLs/commands','Keep the MagicMirror start command and local companion URL in the course notes so you can recover from a broken wake rule.'),A('Do not add motion sensing yet','Continue using touch or keyboard to switch modes until the hybrid software survives a cold reboot.')],['MagicMirror² starts manually.','The local Magic Frame controller opens from 127.0.0.1.','Sleep mode is truly black.','Photo mode works independently.'],'Do not troubleshoot motion sensing, mirror optics, MagicMirror², and photo loading at the same time. Prove each software state separately first.',[T('MagicMirror² is already installed','Skip cloning it again. Run cd ~/MagicMirror && git status, then start it manually to confirm the existing install.'),T('The companion page does not load','Check systemctl --user status pi-project-server and verify the repository path exists.')],[C('Clone official MagicMirror² if missing','if [ -d ~/MagicMirror/.git ]; then echo "MagicMirror already installed"; else cd ~ && git clone https://github.com/MagicMirrorOrg/MagicMirror.git && cd ~/MagicMirror && node --run install-mm && cp config/config.js.sample config/config.js; fi','MagicMirror is installed or the existing install is preserved.'),C('Clone or update companion files',`if [ -d ~/pi-projects/pi-command-center/.git ]; then cd ~/pi-projects/pi-command-center && git pull; else git clone https://github.com/nikkimasani/pi-command-center.git ~/pi-projects/pi-command-center; fi`,'Repository is present.'),C('Install local companion server','cd ~/pi-projects/pi-command-center && bash software/install-local-server.sh','pi-project-server is enabled and running.'),C('Start MagicMirror² manually','cd ~/MagicMirror && node --run start','MagicMirror² opens successfully.')])};
for(const course of courses){for(const phase of course.phases){if(!phase.visual)phase.visual=V(course.scene||'dashboard',phase.title);phase.visual.photo=null;phase.visual.photos=[];}const os=course.phases.find(p=>p.title==='Prepare Raspberry Pi OS and prove the Pi is healthy');if(os)os.visual=V('pi-os','Raspberry Pi Imager customization and the first successful Raspberry Pi OS boot.',null,[{label:'Imager setup',src:'/assets/setup/imager-ssh-generated.jpg',caption:'Raspberry Pi Imager setup reference. Use the written steps for the current Imager version.'},{label:'First boot',src:'/assets/boot-screen.jpg',caption:'Reference for a successful Raspberry Pi OS boot before project software is installed.'}]);if(course.id==='smart-mirror'){const dsi=course.phases.find(p=>p.title.includes('DSI ribbon'));if(dsi)dsi.visual=V('dsi','Use these close-ups only for the DSI ribbon phase.',null,[{label:'Align ribbon',src:'/assets/smart-mirror/dsi-align.jpg',caption:'Align the DSI ribbon straight before closing the latch.'},{label:'Fully seated',src:'/assets/smart-mirror/dsi-seated.jpg',caption:'The DSI ribbon is seated evenly and the latch is closed.'}]);}if(course.id==='magic-frame'){const prereq=course.phases.find(p=>p.title==='Prove both prerequisite modes separately');if(prereq)prereq.visual=V('magic-prereq','Finished Smart Mirror reference used only to verify the prerequisite mirror build.','/assets/smart-mirror-finished-reference.jpg');}if(!course.phases.some(p=>p.title==='Install the common Raspberry Pi software tools')){const osIndex=Math.max(0,course.phases.findIndex(p=>p.title==='Prepare Raspberry Pi OS and prove the Pi is healthy'));const insertAt=osIndex+1;course.phases.splice(insertAt,0,commonFoundation(course));const maker=softwareById[course.id];if(maker)course.phases.splice(insertAt+1,0,maker(course));}}
window.PI_COURSE_V4_ENHANCED='4.0.0';
})();