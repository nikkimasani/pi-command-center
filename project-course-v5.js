(()=>{
'use strict';
const KEY='pi-project-course-v5';
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));
const q=(s,r=document)=>r.querySelector(s),qa=(s,r=document)=>[...r.querySelectorAll(s)];
let db={};try{db=JSON.parse(localStorage.getItem(KEY)||'{}')}catch{}
const save=()=>localStorage.setItem(KEY,JSON.stringify(db));
const state=p=>db[p.id]||(db[p.id]={intro:true,phase:0,step:0,done:{},materials:{},notes:{}});
const getProject=()=>{try{const last=JSON.parse(localStorage.getItem('pi-last-v4')||'null');return (window.PI_PROJECTS||[]).find(p=>p.id===last?.id)||null}catch{return null}};
const isSoftware=a=>/app|dashboard|page|screen|browser|terminal|ssh|command|install|npm|node|git|python|html|css|javascript|config|kiosk|api|vercel|home assistant|photo|timer|weather|calendar|chromium/i.test(a);
const visualId=(p,pi,si)=>`${p.id}/phase-${pi+1}/step-${si+1}.jpg`;
function projectHints(p,a){const t=a.toLowerCase();
 if(p.id==='cyberdeck')return ['Keep the Pi 5, 7-inch display, mini keyboard and portable power physically separate until each works alone.','Dry-fit the enclosure with USB-C, micro-HDMI/DSI, keyboard and charging access visible before fastening anything.','Route cables in broad curves and leave enough slack to open the enclosure without pulling a connector.','Check cooling and port access before closing the final panel.'];
 if(p.id==='electronics-lab')return ['Shut down the Pi and remove USB-C power before moving a jumper wire.','Identify the physical GPIO pin number and the breadboard row before inserting anything.','For LEDs, keep a 220–330Ω resistor in series and verify LED polarity before power.','Build one connection at a time, then trace GPIO → component → GND before restoring power.'];
 if(p.id==='home-panel')return ['Open the Home Assistant dashboard on a normal computer first and verify the account and devices work there.','Design large touch targets for the 7-inch screen and group controls by room or scene.','Test every control in a normal browser before switching the Pi to kiosk mode.','Keep an obvious way to exit kiosk mode or reach the desktop for maintenance.'];
 if(p.id==='ai-terminal')return ['Keep API keys and secrets out of client-side JavaScript and out of the Raspberry Pi browser.','Make the chat UI work with a mock response before connecting a live API.','Test long replies, network failure and empty input before using kiosk mode.','Use a server-side endpoint or hosted backend for any secret-bearing API request.'];
 if(p.id==='pomodoro')return ['Build and test the 25-minute timer in a normal browser before dedicating the Pi display to it.','Make Start, Pause, Reset and Complete easy to hit on the touchscreen.','Confirm the timer survives accidental double taps and cannot go below zero.','Add physical buttons or LEDs only after the software flow is stable.'];
 if(p.id==='photo-frame')return ['Prepare a small test photo folder before copying your entire library.','Use landscape and portrait samples to verify object-fit behavior and black bars.','Test automatic advance, previous/next controls and restart behavior before kiosk mode.','Keep the source photo folder organized so replacing images does not require editing code.'];
 if(p.id==='glance')return ['Decide which information must be readable from several feet away before building the page.','Use large type, strong contrast and only a few cards on the 7-inch screen.','Test each data source separately before combining weather, time, calendar and Pi status.','Run the final dashboard full-screen only after it works reliably in a normal browser.'];
 if(p.id==='magic-frame')return ['Treat the working Smart Mirror as the base system. Do not change hardware and software behavior at the same time.','Test photo reveal and sleep/wake behavior with the frame open before closing it.','Use black backgrounds for hidden states so the mirror effect remains convincing.','If adding sensors or GPIO, validate them separately before connecting them to mirror automation.'];
 return ['Build the page on a normal computer first so layout problems are easier to debug.','Use large touch targets and test the exact 7-inch viewport before kiosk mode.','Keep each feature independent until it works, then combine them.','Verify refresh/restart behavior before dedicating the Raspberry Pi to the project.'];
}
function commands(p,a){const t=a.toLowerCase(),out=[];
 if(/ssh/.test(t))out.push('ssh YOUR_USERNAME@nikkipi.local');
 if(/update|upgrade/.test(t))out.push('sudo apt update','sudo apt full-upgrade -y');
 if(/git/.test(t))out.push('git --version');
 if(/node|npm/.test(t))out.push('node --version','npm --version');
 if(/python/.test(t))out.push('python3 --version');
 if(/temperature|thermal/.test(t))out.push('vcgencmd measure_temp');
 if(p.id==='electronics-lab'&&/gpio|gpiozero/.test(t))out.push('sudo apt install -y python3-gpiozero');
 if(/kiosk|chromium/.test(t))out.push('chromium --kiosk https://YOUR-URL.vercel.app');
 if(p.id==='photo-frame'&&/server|local/.test(t))out.push('python3 -m http.server 8080');
 return [...new Set(out)];
}
function detailed(p,phase,a){const t=a.toLowerCase();let s=[];
 if(/dsi|ribbon/.test(t))s=['Disconnect USB-C power completely and wait until all LEDs are off.','Locate the exact DSI connector before touching its latch.','Open the latch evenly from both sides.','Hold the reinforced end of the ribbon, keep it square, and slide it in with light pressure only.','Check that both edges are inserted to the same depth, then close the latch evenly.','Route the ribbon in a broad curve with no twist, crease or tension.'];
 else if(/gpio|breadboard|resistor|led|button/.test(t))s=projectHints(p,a);
 else if(/mount|enclosure|case|velcro|route|cable|display|power bank|keyboard/.test(t)&&!isSoftware(a))s=['Power the Pi off unless this step explicitly requires a powered test.','Place the named parts on a clean surface and dry-fit them with no adhesive or screws tightened.','Check connector reach, port access, airflow and back-panel clearance from more than one angle.','Secure only the part named in this step. Do not permanently fasten neighboring parts yet.','Gently move or tilt the assembly and confirm nothing shifts, pinches, presses on the LCD or pulls a cable.'];
 else if(isSoftware(a))s=['Open the exact app, terminal or file named in this step.','Make only the change described by the step title before changing anything else.','If a command is provided below, use Copy, paste it once, read it, then press Enter.','Wait for the command or page to finish before continuing. Do not stack commands while output is still running.','Compare the screen or terminal output with the Expected Result below.','If it does not match, stop here and use Troubleshooting before moving on.'];
 else s=projectHints(p,a);
 return s;
}
function expected(p,phase,a){const t=a.toLowerCase();if(/ssh/.test(t))return 'The terminal prompt changes to the Raspberry Pi account, similar to YOUR_USERNAME@nikkipi:~ $.';if(/gpio|breadboard|resistor|led|button/.test(t))return 'The complete circuit path matches the intended GPIO, resistor/component and GND connection before power is restored.';if(/mount|case|enclosure|velcro|route|cable/.test(t))return 'The named part is secure, serviceable, ventilated and does not pinch or strain any cable.';if(isSoftware(a))return 'The screen, page or command completes without a fatal error and the result matches the behavior described by this step.';return phase.success||'The physical or software result matches this step and is safe to continue.'}
function trouble(p,a){const t=a.toLowerCase();if(/ssh/.test(t))return ['Confirm the Pi and computer are on the same network.','Try the Pi IP address if the .local hostname does not resolve.','Recheck the username and password created in Raspberry Pi Imager.'];if(/gpio|breadboard|resistor|led|button/.test(t))return ['Remove power before checking wiring.','Verify the physical GPIO pin, not only the GPIO number.','Check breadboard row continuity, resistor placement and LED polarity.'];if(/display|dsi|ribbon/.test(t))return ['Remove USB-C power before touching the cable.','Reseat both ribbon ends evenly and confirm each latch is closed.','Test the display loose on the desk before mounting it.'];if(isSoftware(a))return ['Read the first meaningful error instead of rerunning the same command repeatedly.','Undo only the most recent change and test again.','Verify network access and file path before reinstalling software.'];return ['Undo only the last change.','Compare the named part or screen with the instruction again.','Do not change several variables at once.'];}
function screenRef(p,a){const label=esc(a),name=esc(p.title);return `<div class="v5-screen"><div class="v5-screenbar">${name} · screen reference</div><div class="v5-screenbody"><b>${label}</b><p>Follow the written actions below. The finished screen should match the function described here, even if your exact text or data differs.</p></div></div>`}
function visual(p,pi,si,a){if(isSoftware(a))return screenRef(p,a);const id=visualId(p,pi,si);return `<div class="v5-visual-pending"><span>📷</span><div><b>Exact step visual</b><p>${esc(a)}</p><code>${esc(id)}</code><small>No unrelated image will be substituted. This fixed ID is the required photo slot for this action.</small></div></div>`}
function activeKey(pi,si){return `${pi}:${si}`}
function intro(root,p,s){const found=Object.values(s.materials).filter(Boolean).length;root.innerHTML=`<div class="v5-wrap"><div class="v5-badge">PROJECT COURSE V5 · DETAILED STANDARD</div><button id="v5Home" class="v5-link">← All projects</button><header><h1>${esc(p.icon)} ${esc(p.title)}</h1><p>${esc(p.summary)}</p></header><section class="v5-card"><h2>How this guide works</h2><p>Each existing project stage is now treated as a phase. Every action has detailed instructions, a visual or fixed visual slot, expected result, troubleshooting and a required verification gate.</p></section><section class="v5-card"><div class="v5-row"><h2>Materials</h2><b>${found}/${p.materials.length}</b></div><div class="v5-materials">${p.materials.map((m,i)=>`<label><input data-m="${i}" type="checkbox" ${s.materials[i]?'checked':''}><span>${esc(m)}</span></label>`).join('')}</div></section><section class="v5-card"><h2>Project roadmap</h2><div class="v5-roadmap">${p.steps.map((x,i)=>`<div><b>Phase ${i+1}</b><span>${esc(x.title)}</span><small>${esc(x.summary||'Complete this phase before moving on.')}</small></div>`).join('')}</div></section><button id="v5Start" class="v5-primary">${s.phase||s.step?'Resume course →':'Start Phase 1 →'}</button></div>`;q('#v5Home',root).onclick=()=>{cleanup();window.renderHome?.()};qa('[data-m]',root).forEach(x=>x.onchange=()=>{s.materials[x.dataset.m]=x.checked;save();intro(root,p,s)});q('#v5Start',root).onclick=()=>{s.intro=false;save();render(root,p,s)}}
function render(root,p,s){s.phase=Math.max(0,Math.min(s.phase,p.steps.length-1));const phase=p.steps[s.phase];s.step=Math.max(0,Math.min(s.step,phase.actions.length-1));const a=phase.actions[s.step],key=activeKey(s.phase,s.step),done=!!s.done[key],cmds=commands(p,a),total=p.steps.reduce((n,x)=>n+x.actions.length,0);let idx=s.step+1;for(let i=0;i<s.phase;i++)idx+=p.steps[i].actions.length;root.innerHTML=`<div class="v5-wrap"><div class="v5-badge">PROJECT COURSE V5 · DETAILED STANDARD</div><div class="v5-crumb">${esc(p.title)} · Phase ${s.phase+1} of ${p.steps.length} · Step ${s.step+1} of ${phase.actions.length}</div><div class="v5-layout"><main><section class="v5-phase"><small>PHASE ${s.phase+1}</small><h2>${esc(phase.title)}</h2><p>${esc(phase.summary||'Finish every checkpoint in this phase before continuing.')}</p></section><article class="v5-step"><small>STEP ${idx} OF ${total}</small><h1>${esc(a)}</h1>${visual(p,s.phase,s.step,a)}<section class="v5-card"><h2>Do this now</h2><ol class="v5-actions">${detailed(p,phase,a).map((x,i)=>`<li><b>${i+1}</b><span>${esc(x)}</span></li>`).join('')}</ol></section>${cmds.length?`<section class="v5-card"><h2>Commands</h2>${cmds.map(c=>`<div class="v5-command"><pre>${esc(c)}</pre><button data-copy="${encodeURIComponent(c)}">Copy</button></div>`).join('')}</section>`:''}<section class="v5-card v5-expected"><small>EXPECTED RESULT</small><p>${esc(expected(p,phase,a))}</p></section><details class="v5-card"><summary>Troubleshooting</summary><ul>${trouble(p,a).map(x=>`<li>${esc(x)}</li>`).join('')}</ul></details><label class="v5-check"><input id="v5Check" type="checkbox" ${done?'checked':''}><span><b>I verified this step</b><small>My result matches the instruction closely enough to continue.</small></span></label><textarea id="v5Note" placeholder="Optional notes for this step">${esc(s.notes[key]||'')}</textarea><div class="v5-nav"><button id="v5Prev" ${idx===1?'disabled':''}>← Previous</button><button id="v5Next" class="v5-primary">${done?(idx===total?'Finish project ✓':'Next →'):'Verify first'}</button></div></article></main><aside class="v5-rail"><h3>Phases</h3>${p.steps.map((x,i)=>`<button data-phase="${i}" class="${i===s.phase?'active':''}"><b>${i+1}</b><span>${esc(x.title)}</span></button>`).join('')}</aside></div></div>`;
 const prev=()=>{if(s.step>0)s.step--;else if(s.phase>0){s.phase--;s.step=p.steps[s.phase].actions.length-1}save();render(root,p,s);window.scrollTo({top:0,behavior:'smooth'})};const next=()=>{if(!s.done[key])return toast('Verify the current result before continuing.');if(s.step<phase.actions.length-1)s.step++;else if(s.phase<p.steps.length-1){s.phase++;s.step=0}else{s.intro=true;save();intro(root,p,s);return toast('Project course complete ✓')}save();render(root,p,s);window.scrollTo({top:0,behavior:'smooth'})};q('#v5Prev',root).onclick=prev;q('#v5Next',root).onclick=next;q('#v5Check',root).onchange=e=>{s.done[key]=e.target.checked;save();render(root,p,s)};q('#v5Note',root).onchange=e=>{s.notes[key]=e.target.value;save()};qa('[data-copy]',root).forEach(b=>b.onclick=async()=>{const c=decodeURIComponent(b.dataset.copy);try{await navigator.clipboard.writeText(c);b.textContent='Copied ✓';setTimeout(()=>b.textContent='Copy',900)}catch{toast('Press and hold the command to copy it.')}});qa('[data-phase]',root).forEach(b=>b.onclick=()=>{s.phase=+b.dataset.phase;s.step=0;save();render(root,p,s)})}
function cleanup(){document.body.classList.remove('v5-active');const v=q('#stepView');if(v){q('#v5Root',v)?.remove();qa(':scope > *',v).forEach(x=>x.style.display='')}}
function mount(){const v=q('#stepView'),p=getProject();if(!v||!v.classList.contains('active')||!p||p.id==='smart-mirror'){cleanup();return}let root=q('#v5Root',v);if(!root){root=document.createElement('div');root.id='v5Root';v.appendChild(root)}document.body.classList.add('v5-active');qa(':scope > *',v).forEach(x=>{if(x!==root)x.style.display='none'});const s=state(p);s.intro?intro(root,p,s):render(root,p,s)}
function toast(msg){let t=q('#v5Toast');if(!t){t=document.createElement('div');t.id='v5Toast';document.body.appendChild(t)}t.textContent=msg;t.classList.add('show');clearTimeout(t._);t._=setTimeout(()=>t.classList.remove('show'),1800)}
const css=document.createElement('style');css.textContent=`body.v5-active .bottom-nav{display:none!important}#v5Root{display:block!important;max-width:1180px;margin:auto;padding:18px;min-width:0}.v5-wrap{color:var(--text);min-width:0}.v5-badge{display:inline-block;padding:5px 9px;border-radius:999px;border:1px solid #285a49;background:#0b281f;color:#8ff0c4;font-size:9px;font-weight:900;letter-spacing:.08em}.v5-link,.v5-nav button{border:1px solid #34465d;background:#101927;color:#fff;border-radius:10px;padding:10px 14px}.v5-wrap header h1,.v5-step h1{font-size:clamp(1.8rem,5vw,3rem);line-height:1.12;margin:14px 0 8px}.v5-wrap header p,.v5-card p,.v5-phase p{color:#a9b6c7;line-height:1.6}.v5-card,.v5-phase,.v5-step{border:1px solid #2d4056;background:#09131f;border-radius:15px;padding:16px;margin:14px 0;min-width:0}.v5-row{display:flex;justify-content:space-between;gap:10px}.v5-materials{display:grid;grid-template-columns:1fr 1fr;gap:9px}.v5-materials label{display:flex;gap:9px;align-items:flex-start}.v5-materials input,.v5-check input{width:20px;height:20px;accent-color:var(--green);flex:0 0 auto}.v5-roadmap{display:grid;gap:8px}.v5-roadmap>div{display:grid;grid-template-columns:75px minmax(0,1fr);gap:4px 10px;padding:10px;border:1px solid #27384c;border-radius:10px}.v5-roadmap small{grid-column:2;color:#8798aa}.v5-primary{width:100%;min-height:48px;border:0;border-radius:11px;background:var(--purple)!important;color:#fff!important;font-weight:900}.v5-crumb{font-size:12px;color:#a990ff;margin:12px 0}.v5-layout{display:grid;grid-template-columns:minmax(0,1fr) 250px;gap:18px;align-items:start}.v5-phase small,.v5-step>small,.v5-expected small{font-size:10px;letter-spacing:.09em;color:#a990ff;font-weight:900}.v5-phase h2{margin:5px 0}.v5-screen,.v5-visual-pending{border:1px solid #3b4f66;border-radius:14px;overflow:hidden;margin:16px 0;background:#07101a}.v5-screenbar{padding:10px 13px;background:#101a28;color:#96a9be;font-size:11px}.v5-screenbody{min-height:180px;display:grid;place-content:center;text-align:center;padding:22px}.v5-screenbody b{font-size:20px}.v5-visual-pending{display:grid;grid-template-columns:44px minmax(0,1fr);gap:12px;padding:15px}.v5-visual-pending span{font-size:28px}.v5-visual-pending p{margin:4px 0}.v5-visual-pending code{display:block;overflow-wrap:anywhere;color:#8dc5ff;font-size:11px}.v5-visual-pending small{display:block;color:#8395a9;margin-top:6px}.v5-actions{display:grid;gap:10px;padding:0;list-style:none}.v5-actions li{display:grid;grid-template-columns:28px minmax(0,1fr);gap:10px;align-items:start}.v5-actions li b{display:grid;place-items:center;width:28px;height:28px;border-radius:50%;background:#1c2b3c}.v5-command{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:8px;margin:8px 0}.v5-command pre{margin:0;overflow:auto;padding:12px;background:#03070c;border:1px solid #26384b;border-radius:9px;white-space:pre-wrap;overflow-wrap:anywhere}.v5-command button{border:1px solid #435873;background:#142238;color:#fff;border-radius:9px;padding:0 12px}.v5-check{display:flex;gap:10px;padding:14px;border:1px solid #315d4c;background:#0b2019;border-radius:12px}.v5-check span,.v5-check small{display:block}.v5-check small{color:#91a79d}.v5-step textarea{width:100%;min-height:72px;margin:12px 0;background:#07101a;border:1px solid #304157;border-radius:10px;color:#fff;padding:10px}.v5-nav{display:flex;gap:10px;justify-content:space-between}.v5-nav .v5-primary{width:auto;padding:0 22px}.v5-rail{position:sticky;top:14px;border:1px solid #2d4056;background:#09131f;border-radius:14px;padding:12px}.v5-rail button{display:flex;width:100%;gap:8px;align-items:flex-start;text-align:left;padding:9px;border-radius:9px;color:#aab8c8}.v5-rail button.active{background:#20184a;color:#fff}.v5-rail button b{min-width:20px}#v5Toast{position:fixed;left:50%;bottom:22px;transform:translate(-50%,20px);opacity:0;background:#111b29;color:#fff;border:1px solid #38506b;border-radius:10px;padding:10px 14px;z-index:9999;transition:.2s}#v5Toast.show{opacity:1;transform:translate(-50%,0)}@media(max-width:760px){#v5Root{padding:10px;max-width:100%;overflow-x:hidden}.v5-layout{display:block}.v5-rail{position:static;display:flex;overflow-x:auto;gap:6px;margin:10px 0}.v5-rail h3{display:none}.v5-rail button{min-width:150px;border:1px solid #263a50}.v5-materials{grid-template-columns:1fr}.v5-roadmap>div{grid-template-columns:65px minmax(0,1fr)}.v5-step,.v5-card,.v5-phase{padding:13px}.v5-command{grid-template-columns:1fr}.v5-command button{min-height:42px}.v5-nav{position:sticky;bottom:8px;background:#09131fee;padding:8px;border-radius:12px}.v5-step h1{font-size:clamp(1.55rem,7vw,2.25rem);overflow-wrap:anywhere}}`;
document.head.appendChild(css);
new MutationObserver(()=>requestAnimationFrame(mount)).observe(document.body,{subtree:true,childList:true,attributes:true,attributeFilter:['class']});
setTimeout(mount,350);
})();
